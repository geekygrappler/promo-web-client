<ul>
  {{#each constraintsWithoutPromotionPeriodConstraint as |constraint|}}
    <li>
      {{constraint-display 
        constraint=constraint 
        minimumBasketTotal=minimumBasketTotal
      }}
    </li> 
  {{/each}} 
</ul> 

{{yield}} 